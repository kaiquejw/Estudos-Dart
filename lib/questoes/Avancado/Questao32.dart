// 32. Como você gerencia e versiona suas dependências do Dart usando pubspec.yaml?

class GerenciadorDependencias {
  String definicao() => 'Defino e versiono pacotes no pubspec.yaml.';
  
  List<String> comandosEssenciais() {
    return [
      'flutter pub get (instalar)',
      'flutter pub upgrade (atualizar)',
      'Garantia de compatibilidade e estabilidade'
    ];
  }
}